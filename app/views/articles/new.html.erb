<%= stylesheet_link_tag('articleIndex')%>
 <% render 'shared/errors', obj: @article %>
<div class="container-fluid">
	<%= render '/shared/errors' %>
	<div class="row">
	  <div class="col-md-4"></div>
	  <div class="col-md-4"><br>
	  	<h2><center>Create New Article</center></h2><hr>
	  	<div class="card">
	  		<div class="card-body">
	  			<%= form_with model: @article do |f| %>

<p>
	<%= f.label :title %><br>
	<%= f.text_field :title, class: "form-control"  %>
</p>

<p>
	<%= f.label :description%><br>
	<%= f.text_area :description, class: "form-control"  %>
</p>
<p>         <%= f.label :Categories%><br>
            <%= f.collection_select(:category_ids, Category.all, :id, :name,
            { prompt: "Make your selection from the list below (can be empty)"}, { multiple: true, size: 3, class: "custom-select form-control" }) %>
</p>
<!-- <p>
<select class="form-select" aria-label="Default select example" multiple="multiple">
  <option selected>Open this select menu</option>
  
  <% categories = Category.all %>
  <% categories.each do |category| %>
    <option value="<%= category.id %>"><%= category.name %></option>
  <% end %>
</select>

</p> -->


<div>
	<%= f.submit 'Add Article', class: "btn btn-outline-danger" %>
	<%= link_to 'Return To Articles list', articles_path, class: "btn btn-outline-success" %>
</div>


<% end %>
	  		</div>
	  	</div>

	  </div>
	  <div class="col-md-4"></div>
    </div>
</div>	